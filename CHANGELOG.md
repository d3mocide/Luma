# Changelog

All notable changes to the Luma health tracking and meal-planning PWA will be documented in this file.

## [Phase 3] - Polish (Active)

### Changed
- **Mobile Budget Checker Metric Layout**: Changed the Calories, Saturated Fat, and Soluble Fiber budget check stats layout on mobile viewports to display on a single line (side-by-side). Tweaked font sizes (label: 10.5px, number: 18px, unit: 11px, footer: 10.5px) and spacing to improve readability on small viewports. Positioned the serving size input and "+ Add to log" button side-by-side on the same row using a flex-stretch layout, arranged all 5 preset chips in a single clean row directly underneath them, hid the helper placeholder text ("Search for a food to check your budget.") on mobile when no food is selected to reduce layout clutter, and styled the status message under a subtle hairline top border.
- **Today Page Insight Card Stretched Flex Layout**: Restored the Budget Checker (Nutrition Calculator) to its full-width position above the bottom grid layout. Set the Insight card to stretch to match the height of Today's plan card, and styled it as a flex column with `marginTop: 'auto'` on the button footer. This pushes the buttons to the bottom of the card, filling the vertical card space elegantly on desktop. Refactored the mobile Insight card layout from a side-by-side flex split to a clean vertical block layout matching the desktop hierarchy, aligning the Sparkles icon on a header row next to the severity tag and allowing the text content to span the full width of the card for maximum legibility on narrow viewports.
- **Insight Card Accent Borders Removed**: Completely removed left borders/indicator bars from all Insight cards (desktop Today page, mobile Today page, and Coach tab) to achieve a cleaner, completely flat glassmorphic card appearance.
- **Mobile Settings Panel Positioning**: Shifted the mobile settings dropdown panel (`.mobile-profile-panel`) down by adjusting its offset to `top: calc(100% + 20px)` to match the 20px standard layout spacing and prevent layout overlap.
- **Today Page Insight Card Expansion**: Expanded the Today page's insight card to display the detailed narrative body text (`active_insight.cta`) beneath the short headline on both desktop and mobile viewports. Styled the body text with appropriate theme-adaptive typography (`var(--fg-secondary)`) and spacing (8px desktop / 6px mobile) to ensure a rich, informative visual layout that adheres strictly to the `refrence/BRAND-GUIDE.md` specifications.
- **Mock Insight Data Structure Correction**: Updated the mock `active_insight` structure in `frontend/src/lib/mock-data.ts` to properly split the short headline (`"Great fiber momentum."`) from the detailed narrative body (`"You have excellent fiber momentum this week. Keep saturated fat under 18g today to reinforce LDL progress."`), matching the true database narrative structure generated by the `insight_narrator` model.

## [Phase 1] - Ingestion & Meal Planning (Active)

### Fixed
- **Local AI Reasoning Output Normalization**: Fixed an issue where local reasoning models (like `gemma-4-e4b-it`) return their output in the `reasoning` or `reasoning_content` field while leaving the `content` field empty. Implemented a robust `_normalize_reasoning_response` interceptor in the LLM client wrapper ([llm_client.py](file:///d:/Projects/Luma/backend/luma/services/llm_client.py)) that copies reasoning content to the `content` field, preventing parsing failures and fallback stubs (like "New insight"). Created a unit test suite in [test_llm_client.py](file:///d:/Projects/Luma/backend/tests/test_llm_client.py).
- **HAE Webhook Verification Reverted to Secure Static Secret**: Reverted the body-level HMAC-SHA256 signature verification in `_verify_app_secret` ([ingest.py](file:///c:/Projects/Luma/backend/luma/api/ingest.py)) to a secure static header comparison against `settings.hae_shared_secret` using constant-time `hmac.compare_digest`. The Health Auto Export app natively transmits static custom headers configured by the user, rather than dynamic body signatures.
- **HAE Import Normalizer Audited for Key Variations**: Audited and expanded `HAE_METRIC_MAP` ([hae_normalizer.py](file:///c:/Projects/Luma/backend/luma/services/hae_normalizer.py)) to include `weight_&_body_mass`, `weight`, and `body_mass` mappings (pointing to the internal `weight_kg` metric), and added `oxygen_saturation` mapping to `spo2_pct` to handle regional/app version variations.
- **Duplicate Request Conflict Fix in verify_api.py**: Updated E2E verification webhook ingest test `_check_ingest_signed_hae` ([verify_api.py](file:///c:/Projects/Luma/backend/verify_api.py)) to pass a unique timestamp parameter inside the JSON payload. This prevents replay protection checks in Redis from throwing a `409 Conflict` error when executing the E2E verification test multiple times in quick succession.
- **Insights & Alert Engine Postgres Syntax Error**: Fixed a PostgreSQL syntax error in the alert engine query ([engine.py](file:///c:/Projects/Luma/backend/luma/alerts/engine.py)) where a Postgres-style type cast on the bound parameter (`:payload::jsonb`) was misinterpreted by SQLAlchemy as an invalid parameter name (`:jsonb`), causing a database crash and preventing any insights from being generated or displayed. Replaced the expression with `CAST(:payload AS JSONB)` and created a comprehensive test suite in [test_alerts.py](file:///c:/Projects/Luma/backend/tests/test_alerts.py) verifying alert rules, alert engine execution, and narration workflows.
- **Search Tab Filter Chips Wrapping and Icons**: Configured the search filter chips layout container in the meal logging panel ([IngredientBuilder.tsx](file:///c:/Projects/Luma/frontend/src/components/log-sheet/IngredientBuilder.tsx)) to wrap (`flexWrap: 'wrap'`), removed the horizontal scrolling container, and added premium geometric Lucide SVG icons (Heart, Shield, Wheat, Dumbbell, Sprout, Flame) to each chip with aligned layouts, ensuring all chips fit cleanly in multi-line flow without horizontal scrollbar clutter.
- **Log Panel Favorites Tab Relocation**: Relocated the "My favorites" card/list from the **Voice** tab to the **Search** tab inside the global meal logging panel ([LogSheet.tsx](file:///c:/Projects/Luma/frontend/src/components/LogSheet.tsx)). Refactored props and callbacks across [VoiceTab.tsx](file:///c:/Projects/Luma/frontend/src/components/log-sheet/VoiceTab.tsx) and [SearchTab.tsx](file:///c:/Projects/Luma/frontend/src/components/log-sheet/SearchTab.tsx), rendering the favorites list below the IngredientBuilder under a clean separator hairline border with a premium glass-inset design.
- **Favorite Meal Logging Confirmation Modal**: Added a premium glassmorphic overlay confirmation modal (`successModal` state and overlay UI in [favorites.tsx](file:///c:/Projects/Luma/frontend/src/routes/favorites.tsx)) with a green checkmark icon to confirm the target meal and slot when direct favorite logging completes successfully, preventing operators from accidentally double-logging.
- **Direct Favorite Meal Logging and Systematic Naming**: Replaced the multi-step intermediate logging panel flow with direct favorite meal logging from the Favorites page and the log panel's favorites chips. Created a clean, dynamic client-side `getCurrentSlot` utility that guesses the correct meal slot based on the local time of day. Unified the logged source type as `"favorite"` and modified the backend `get_today` endpoint to systematically pass through the custom favorite's name as the logged meal headline, matching the brand guidelines.
- **Modal and Drawer Scrollbar Polish**: Addressed visual inconsistency by applying Luma's premium, custom `.thin-scroll` class to the scrollable container wrappers inside the Journal Logging Drawer ([JournalDrawer.tsx](file:///c:/Projects/Luma/frontend/src/components/journal/JournalDrawer.tsx)) and the Meal Plan Slot Replacement Modal ([SlotModal.tsx](file:///c:/Projects/Luma/frontend/src/components/plan/SlotModal.tsx)), eliminating thick, jarring native browser scrollbars.
- **Journal Drawer Sticky Header Collision**: Patched a visual overlap bug in the Journal Logging Drawer by refactoring its header container into a dedicated, theme-adapted `.drawer-header-sticky` element. Applied a `backdrop-filter: blur(20px)` and a slightly opaque (`94%`) background fill color (`rgba(13, 20, 37, 0.94)` / `rgba(255, 255, 255, 0.94)` depending on light/dark theme) along with a subtle hairline bottom border. This creates a frosted-glass barrier that blurs and masks input fields as they scroll underneath, completely resolving the overlapping text collision.
- **Search Cancel Button Collision**: Suppressed browser-native search cancel (`::-webkit-search-cancel-button`) and decoration buttons on search inputs, eliminating the double-clear button collision with Luma's custom glassmorphic clear button inside the Meals page food database search bar.
- **Curated Search Source Labeling**: Integrated custom, high-fidelity glassmorphic source label badges (e.g. `USDA Reference`, `Custom`, `USDA API`, `Open Food Facts`) across all food search result views—including the main Meals route database list, the weekly meal plan Slot Replacement Modal, and the Today dashboard's Budget Checker calculator dropdown—making it instantly clear to the operator which entries contain clinical-grade core nutrients.
- **Curated Reference Foods Search Priority**: Optimized the food search database query in [foods.py](file:///c:/Projects/Luma/backend/luma/api/foods.py) with dynamic word-boundary regular expression matching and singular/plural stemming. This prevents unrelated USDA Reference foods (like Scallops or Oats) from ranking above exact/word matches containing the query term (like Steak), ensuring high-fidelity curated ingredients always surface at the very top.
- **Pending Journal Ingestion Crash**: Patched the `/api/v1/journal/pending` endpoint in [journal.py](file:///c:/Projects/Luma/backend/luma/api/journal.py) to make the meal name headline resolver robust to both dictionary-formatted items (from production food logs) and string-formatted items (from seeded mock databases), preventing a `500 Internal Server Error` and `AttributeError: 'str' object has no attribute 'get'` crash.
- **App Routing Missing Import**: Added the missing `Navigate` component import from `react-router-dom` in [App.tsx](file:///c:/Projects/Luma/frontend/src/App.tsx), resolving a compiler check error and runtime `ReferenceError: Navigate is not defined` crash on the default index route.
- **Vite Watcher Event Loop Hang**: Resolved a critical dev infrastructure bug where Vite's watcher (Chokidar) would recursively scan and watch the large `./frontend/.pnpm-store` directory on startup. Because `.pnpm-store` contains thousands of package files bind-mounted from the host (where file system operations are exceptionally slow on Windows/WSL), this was consuming 100% of a CPU core and completely blocking Node's event loop, causing connection attempts to `http://localhost:5173` to hang and time out. Added `**/node_modules/**`, `**/.pnpm-store/**`, and `**/dist/**` to the `server.watch.ignored` array in [vite.config.ts](file:///c:/Projects/Luma/frontend/vite.config.ts), restoring instant server response times and reducing CPU consumption from over 100% to under 1%.
- **AI Telemetry Provider Parsing Bug**: Resolved a bug in `llm_client.py` where telemetry metrics recorded a static `"native"` or `"openai"` provider because of relying solely on missing `custom_llm_provider` payload flags. Added a robust `_get_provider` helper that parses the active provider prefix directly from the model alias (e.g. `gemini/`, `anthropic/`, `local/`), perfectly aligning backend events with frontend performance indicators ('gemini', 'anthropic', 'local').
- **SQL Transaction bug fix**: Replaced Postgres double-colon type casting operators (`::text`, `::float`, `::date`) with standard SQL `CAST(...)` syntax within raw SQL blocks inside the Coach agent and Trends API, resolving the `InFailedSQLTransactionError` transaction failure during tool execution.
- **HAE HMAC Webhook Signatures**: Corrected Health Auto Export (HAE) webhook endpoints to properly read the request body bytes and calculate the HMAC-SHA256 signature using the shared application secret, resolving the 401 Unauthorized signature verification mismatch during testing.
- **AppShell Compiler Fix**: Added the missing `useLayoutEffect` React import in [AppShell.tsx](file:///c:/Projects/Luma/frontend/src/components/AppShell.tsx) to resolve TypeScript compilation error `TS2304`.
- **Today Weight Sparkline Restoration**: Added dynamic React Query hooks fetching live 30-day weight trends (`/trends/weight_kg?range=30d`) inside [today.tsx](file:///home/zbrain/Projects/Luma/frontend/src/routes/today.tsx), automatically mapping them to the `<WeightChart>` component and eliminating the blank card space. Provided a seamless mock-series fallback for day 1 users to guarantee a premium visual layout.
- **Modal Overlay Layering Bug**: Removed `zIndex: 1` from the main content container in [AppShell.tsx](file:///home/zbrain/Projects/Luma/frontend/src/components/AppShell.tsx) to prevent it from establishing an isolated local stacking context. This ensures that both the Meal Plan Slot details/replacement modal and the plan generation progress overlays correctly layer on top of the Desktop Sidebar (`zIndex: 2`) and Mobile Header, fully dimming the sidebar buttons and preventing accidental double clicks or navigation during active processes.
- **Today Budget Calculator Dropdown Bug**: Refactored the food search results dropdown inside [NutritionCalculatorCard.tsx](file:///home/zbrain/Projects/Luma/frontend/src/components/today/NutritionCalculatorCard.tsx) to be absolutely positioned rather than in-flow. Applied the premium `.glass-bright` class to the floating panel. This prevents search results from pushing down the search card layout, completely resolving the vertical stretching issue on the neighboring "Serving (g)" input container and keeping the layout perfectly aligned.
- **Meal Search Weight Preset Chips**: Integrated serving-size preset buttons (`50g`, `100g`, `150g`, `200g`, `300g`) with beautiful sky blue active states matching the Luma visual guide directly into the Meal Logging Search / Plate tab list inside [PlateTab.tsx](file:///home/zbrain/Projects/Luma/frontend/src/components/log-sheet/PlateTab.tsx).
- **Budget Check Serving Presets & Layout Polish**: Added identical serving-size preset buttons (`50g`, `100g`, `150g`, `200g`, `300g`) to the Today dashboard budget checker calculator card inside [NutritionCalculatorCard.tsx](file:///home/zbrain/Projects/Luma/frontend/src/components/today/NutritionCalculatorCard.tsx). Re-architected the input container from a `<label>` to a semantic `<div>` to prevent annoying focus-stealing bugs, added border active focus animations on the number input, and implemented premium custom hover styles for inactive button states.

### Added
- **LLM Telemetry Unit Test Suite**: Added a dedicated test suite (`test_llm_metrics.py`) and updated `conftest.py` with mock stubs to validate token tracking, model-specific aggregation, and snapshot outputs.
- **Client-Side Image Compression for Photo Logging**: Implemented a browser-based canvas compression routine in `PhotoTab.tsx` that downscales captured food photos to fit within a 1024px bounding box and compresses them to an 80% quality JPEG blob, reducing upload sizes by 97% (~150KB instead of ~5MB) to speed up mobile network transfers.
- **Client-Side Bitrate Constraints for Voice Logging**: Configured `MediaRecorder` in `VoiceTab.tsx` to record audio notes at 32 kbps (`audioBitsPerSecond: 32000`). This maintains perfect Whisper transcription accuracy while reducing 10-second audio sizes to under 40KB.
- **Automatic Reference Database Seeding**: Integrated a database check during the API server startup `lifespan` inside [main.py](file:///c:/Projects/Luma/backend/luma/main.py). If the `foods` table is completely empty (e.g. after a fresh installation or stack rebuild with `make nuke`), the server automatically runs the high-fidelity `ingest_usda` reference seeder in the background. Added a corresponding `make seed-reference` manual target to the [Makefile](file:///c:/Projects/Luma/Makefile) for forcing manual re-seeding when desired, and updated the [README.md](file:///c:/Projects/Luma/README.md) documentation.
- **Temporary Password Change Enforcement & Password Rotation**:
  - Implemented secure temporary password detection by adding the `is_password_temp` column to the `User` database model and creating an Alembic schema migration (`0010_add_is_password_temp`).
  - Added the `/api/v1/auth/change-password` backend endpoint which validates new passwords (requiring a minimum of 8 characters), verifies the current password against the stored hash, and clears the temporary flag.
  - Automatically enforce the password change flow on initial login by rendering a gorgeous, full-screen **Atmospheric** change password prompt in `AppShell.tsx` that blocks dashboard access until a secure password is set.
  - Created a new Settings card `PasswordCard.tsx` in `Settings -> Account` allowing any logged-in user to rotate and change their own password securely at any time.
- **AI Call Price Calculator Widget**: Added an interactive, high-fidelity pricing calculator inside the AI Routing tab (rendered underneath the active LLM activity log card) to model calls per day and token payload sizing. Dynamically reads the active server model routing configurations, maps their provider tiers automatically, and allows operators to customize/edit pricing rates manually with persistent database storage in PostgreSQL via secure REST endpoints to sync across all devices (PWA, mobile, and desktop) as real-world LLM prices fluctuate over time.
- **Full-Screen Settings Layout**: Removed the 1180px maximum-width limits from settings headers, tab strips, and grids in CSS, allowing the account, webhook import, and AI model telemetry views to span the entire screen width on wider desktop viewports.
- **AI Model Routing Diagnostics Card**: Surfaces the active primary and fallback LLM model associations (e.g. Claude Sonnet, Gemini Flash, or Local Llama) mapped across Luma's 5 core health intelligence services (`meal_planner`, `coach_agent`, `food_extractor`, `vision_classifier`, and `insight_narrator`) and registers connection endpoints (`local_ai_api_base`, `whisper_url`) securely on the server. Removed row SVG icons for a highly simplified, ultra-premium text grid appearance.
- **AI Provider Performance & Telemetry Card**: Aggregates the 20 most recent LLM events to calculate and display real-time latency, payload density (average tokens), and success ratios dynamically per provider (Gemini, Anthropic, Local), rendering custom glassmorphic progress bar charts. Positioned at the top of the secondary layout column (above the routing config card) to ensure primary visibility of real-time server diagnostics.
- **Tabbed Longevity & Gait Trends**: Organized the 27 Apple Health biometrics into four high-fidelity clinical tabs (**Recovery & Sleep**, **Activity & Energy**, **Gait & Posture**, and **Vitals & Composition**).
- **Primary Goal Anchor**: Kept the primary **Weight** card anchored at the top of the page as a persistent progress hero chart, while the glass tab bar dynamically filters the grid of supporting biometrics beneath it.
- **Precision Metric Formatting**: Added specialized format precision mapping to ensure high-fidelity gait parameters (e.g. asymmetry percentage and stair speeds in m/s) display with accurate decimal resolution.
- **Seeded 17+ Longevity & Gait Biomarkers**: Upgraded the mock data seeding pipeline to generate realistic timeseries data for all 17+ new biometric metrics (such as walking speed, asymmetry, step length, breathing disturbances, daylight exposure, etc.), fully populating the clinical hubs instantly.
- **User ID UI Display**: Added a copyable **User ID** row under the Account card in the Settings panel with dynamic glassmorphic copy buttons, enabling seamless retrieval of operator UUIDs for administration and local seeding tasks.
- **Mock Data Seeding Pipeline**: Created an administrative utility script ([seed_mock_data.py](file:///home/zbrain/Projects/Luma/backend/luma/scripts/seed_mock_data.py)) and matching `make seed-mock` command. It allows instant generation of 30-day weight trends, LDL cholesterol trends, daily macro budgets, and 14 days of compliance/indulgent meals for any user UUID (auto-creating the user and executing TimescaleDB continuous aggregate refreshes dynamically).
- **Seeded Complete Biometrics**: Expanded [seed_mock_data.py](file:///home/zbrain/Projects/Luma/backend/luma/scripts/seed_mock_data.py) to seed full biometric dimensions (`hrv_ms`, `rhr_bpm`, `sleep_duration_min`, `sleep_score`, `steps`, `active_kcal`), fully populating the "BIOMETRICS - LAST NIGHT" section on the Today page dashboard.
- **Expanded Biometrics Grid**: Added high-value tiles for **Exercise** (minutes) and **Sleep Respiratory Rate** (breaths/min) into both the desktop and mobile biometrics grids in [today.tsx](file:///home/zbrain/Projects/Luma/frontend/src/routes/today.tsx). This scales the layout to an aesthetically perfect 4x2 grid on both viewports, utilizing custom Lucide icons and dedicated theme-adapted colors.
- **Biometrics Seeding Extension**: Updated [seed_mock_data.py](file:///home/zbrain/Projects/Luma/backend/luma/scripts/seed_mock_data.py) to seed both `exercise_min` and `respiratory_rate_bpm` records across the 30-day timeline.
- **Expanded Trends Metrics & Dynamic Icons**: Integrated **Exercise** and **Respiratory Rate** trends charts into [trends.tsx](file:///home/zbrain/Projects/Luma/frontend/src/routes/trends.tsx), utilizing the pre-populated database views. Added a dynamic icon-renderer inside `MetricChart` to display custom Lucide icons (Timer, Wind, Moon, Heart, Flame) inside small trends cards instead of a generic activity symbol. This completes the layout into a gorgeous, highly dense, symmetrical 3x2 grid.
- **Premium Meal Planner Orchestration Overlay**: Added a high-fidelity frosted glass modal overlay with an active progress bar and explicit caution notification ("⚠️ Do not leave or refresh this page") that locks the UI while the meal plan agent orchestrates a new 7-day plan, protecting workflow integrity in both the generation and regeneration states.
- **Mobile Sparkline Enhancement**: Added the compact weight sparkline to the mobile Weight card layout in [today.tsx](file:///home/zbrain/Projects/Luma/frontend/src/routes/today.tsx), elevating mobile visual fidelity and maintaining strict aesthetic parity with the desktop dashboard.
- **Per-User HAE Import Tokens (Migration 0004)**: Health Auto Export ingestion is now fully multi-user safe. Each account receives a unique webhook URL (`POST /ingest/hae/{token}`) that both identifies and authenticates the user — no more first-operator-wins ambiguity. The global `HAE_SHARED_SECRET` is retired. A new **Health import** card in Settings displays the user's personal webhook URL with a one-click copy button and a guarded **Regenerate URL** action. Replay protection is scoped per-user so identical payloads from different accounts never collide.
- **LLM Performance & Cost Telemetry**: Added a robust tracking service (`llm_metrics.py`) that captures prompt/completion tokens, processing latency, cache hit efficiency, and cost across all LLM requests, exposing a detailed Telemetry Dashboard on the Settings page displaying usage and budget charts.
- **Goal Settings Management**: Enhanced the Settings page to support operator-scoped target settings for Target Weight, LDL Cholesterol limits, and dietary patterns, with robust validation and success/error message feedback.
- **PWA Caching & Offline Capabilities**: Integrated a registered service worker utilizing `vite-plugin-pwa` and `workbox-window` to support precaching and standalone offline usability.
- **Food Browser in Meal Planner**: Added an in-modal food browser that lets users replace any planned meal slot with a real food from the database. Search by name, select, enter serving size in grams, preview live nutrition (calories, sat fat, soluble fiber, protein), and confirm — updates the slot via `POST /plan/slot/{id}/replace`.
- **Day Nutrition Totals on Calendar**: Each day card in the weekly calendar now shows a footer with per-day totals for calories, saturated fat, and soluble fiber, computed server-side and returned in `GET /plan/current` as `day_totals`.
- **Real Slot Nutrition in Modal**: The slot detail modal now displays actual agent-estimated (or food-database-sourced) nutrition instead of hardcoded placeholder values.
- **Persisted Shopping List Checkmarks**: Clicking a shopping list item now calls `PATCH /plan/{id}/shopping-list/{food_id}` to persist the purchased state to the database, with an optimistic local override for instant feedback.
- **Slot Nutrition Persistence (Migration 0003)**: Added `nutrition JSONB` and `food_id UUID FK→foods` columns to `meal_plan_slots` so agent-calculated nutrition survives across sessions.
- **USDA FoodData Central Fallback**: `/foods/search` now falls back to the live USDA FDC API when local results are sparse (< 5 hits), caches new foods into the local database, and returns a unified ranked result set.

### Changed
- **AI Cost Analysis & Telemetry Redesign**: Repurposed the synthetic AI Price Calculator into a telemetry-backed Actual Cost Analysis widget (`AiPriceCalculator.tsx`). Integrates with the `llm-metrics` endpoint to group token consumption by model, compute real costs using default/custom pricing rates, and pre-load interactive projection sliders with actual average tokens per call. Surfaced split input/output token counts (`X in · Y out`) in the activity logs (`LlmMetricsCard.tsx`).
- **Trends Wellbeing Tab Grid Layout**: Refactored the outer container in `WellbeingTab` inside [trends.tsx](file:///c:/Projects/Luma/frontend/src/routes/trends.tsx) to use Luma's native `.trends-metric-grid` CSS class. This replaces the single-column flex list layout, aligning the wellbeing trends (Energy, Digestion, Mood, Satiety) with the aesthetic two-column layout format of the other biometric categories on widescreen desktop viewports, while remaining fully responsive and collapsing to a single-column layout on smaller screens.
- **Meals Page Widescreen Layout**: Removed the hardcoded `maxWidth: 960` constraint and centered margin limits from `MealsRoute` in [meals.tsx](file:///c:/Projects/Luma/frontend/src/routes/meals.tsx). Defined a fully responsive `.meals-page` layout class in [index.css](file:///c:/Projects/Luma/frontend/src/index.css) to support dynamic fluid padding (`32px` on desktop, `26px` on tablet, `16px` on mobile), matching the visual pacing and widescreen desktop fluidity of the Today and Trends dashboards.
- **Default Plan Tab Configuration**: Set the **Plan** weekly calendar tab as the default route view when accessing the `/meals` path without an explicit search query parameter, and cleaned up tab query param updates to yield short, elegant root URLs.
- **Nested Layout Optimization**: Eliminated the redundant negative-margin nesting hack (`margin: '0 -20px'`) around `<PlanRoute />` inside [meals.tsx](file:///c:/Projects/Luma/frontend/src/routes/meals.tsx) and streamlined `.plan-page` CSS layouts in [index.css](file:///c:/Projects/Luma/frontend/src/index.css), completely preventing duplicate paddings and scrollbar containers while achieving visual harmony across all tab views.
- **Plan Tab Header Consolidation**: Resolved a double-header layout clutter issue in the Meals page. When rendering the Plan tab ([plan.tsx](file:///c:/Projects/Luma/frontend/src/routes/plan.tsx)), removed the duplicate large subtitle header block ("Your heart-healthy week") and its target stats description, keeping the layout clean and fully focused. Preserved and restored the clean, compact eyebrow sub-header (e.g. `WEEK OF MAY 30 - JUN 5` or `Past week — ...`) at the top of the tab container, retaining important temporal context without cluttering the screen. Preserved all essential weekly navigation, grocery shopping controls, and AI regeneration triggers, integrating them cleanly into the Meals parent viewport rhythm.
- **Journal Drawer Design Harmonization**: Redesigned and overhauled the layout and positioning of the Journal Logging Drawer ([JournalDrawer.tsx](file:///c:/Projects/Luma/frontend/src/components/journal/JournalDrawer.tsx)) to match the exact premium rendering and layout structure of the global "Log a Meal" drawer (`LogSheet`). Positioned it as a full-height, right-aligned sliding panel (`width: 100%`, `maxWidth: 480px`) with a clean left glass border, custom atmospheric glow backdrop (`.log-sheet-atmo`), a matching header layout with Luma's animated pink-shaded close button, a flexible scrollable body, and a sticky bottom footer holding the "Save journal entry" action. This delivers perfect layout harmony across all multi-modal log inputs.
- **Dev Container Hot Reloading Fix**: Enabled containerized dev hot reloading by default by setting `CHOKIDAR_USEPOLLING=1` in [compose.yml](file:///c:/Projects/Luma/compose.yml), and documented the settings inside [.env](file:///c:/Projects/Luma/.env) and [.env.example](file:///c:/Projects/Luma/.env.example). Since inotify file system notifications from Windows/macOS do not natively propagate across NTFS volumes to container bind-mounts, this forces Vite to fallback to file polling, enabling immediate hot module replacement (HMR) during developer updates.
- **Responsive Food Groups Grid Layout**: Refactored the food groups browser layout on the Meals page into a responsive three-column grid (`.food-category-grid`) on desktop viewports. This matches Luma's maximum `960px` layout boundary and expands card widths from `221px` to `298px` to give titles like "Legumes & beans", "Whole grains", and "Nuts & seeds" room to breathe. Enforced a `white-space: nowrap` guard on the sat fat level badge (`SatFatBadge`) to prevent internal badge wrapping and vertical stacking on narrower screens.
- **Responsive Navigation Menu Order**: Separated the navigation item arrays for desktop and mobile viewports. On desktop sidebars, Today now renders first (followed by Plan, Trends, and Coach) to match daily operator focus, while the mobile bottom navigation bar preserves its custom balanced item spacing order (Plan, Today, FAB, Trends, Coach).
- **Premium In-Modal Food Browser UX**: Refactored the meal planner replacement panel in [SlotModal.tsx](file:///home/zbrain/Projects/Luma/frontend/src/components/plan/SlotModal.tsx) to feature beautiful transition animations, focus glowing highlights, dynamic search result rows with highlight hovers, a dedicated glass-framed detail card for selected foods, and interactive serving preset chips (`50g`, `100g`, `150g`, `200g`, `300g`) for instant serving quantity assignment, fully optimized with theme-adaptive styling to prevent high-contrast readability bugs in light mode.
- **Fully Responsive Trend Sparklines**: Refactored the `<Spark>` component in [Spark.tsx](file:///home/zbrain/Projects/Luma/frontend/src/components/ui/Spark.tsx) to use dynamic percentage widths (`width="100%"`) and stretching rules (`preserveAspectRatio="none"`). This enables all biometric sparkline graphs on the Trends page to scale smoothly and occupy the entire width of their parent card containers rather than stopping abruptly in the center.
- **Streamlined Coach Chat Onboarding**: Removed the redundant lower suggestion chips (`SUGGESTION_CHIPS`) hovering above the chat input field on the Coach page, keeping the user interface clean and focusing attention on the primary central prompt suggestion grid when starting a new thread.
- **iOS Standalone PWA Notch & Safe Area Support**: Replaced mobile floating navigation with a fixed frosted-glass `MobileHeader` utilizing native safe-area inset environment variables (`env(safe-area-inset-top)`). Shifted the viewport layouts of all five route pages to prevent top system bars, notches, or Dynamic Islands from obscuring app titles and navigation controls.
- **Fidelity-Correct Shopping List References**: Refactored shopping list item mapping and lookup APIs to cleanly associate items by unique database Food IDs rather than loose title text, ensuring perfect correlation between plan slots, recipe ingredients, and shopping checks.
- **Responsive Calendar Column Compression**: Tuned mobile and tablet layout grid breakpoints inside `index.css` to allow calendar day column text and slot layouts to compress gracefully without horizontal breaks.
- **Fix: Log-as-Eaten Dummy Nutrition**: `POST /plan/{id}/log-as-eaten/{slot_id}` now uses the slot's persisted `nutrition` field instead of hardcoded 350 cal / 1g sat fat placeholder values.
- **Meal Plan Generation Preserves Nutrition**: `POST /plan/generate` now stores the agent's per-slot nutrient estimates in the database rather than discarding them.
- **Slot Replace Replaces Swap**: The old `POST /plan/slot/{id}/swap` (AI-generated random alternative) is replaced by `POST /plan/slot/{id}/replace` with an explicit food and serving size from the food browser.

- **Refined Today Panel Layout (Desktop & Mobile)**: Optimized the Today dashboard layouts for both desktop and mobile. On desktop, combined the Weight, Rings, and Streak cards into a single cohesive 2x2 CSS Grid container with the Weight card spanning two vertical rows and all margins aligned to a strict 20px rhythm. On mobile, re-ordered all cards into a premium, highly logical priority sequence (Rings → Weight → Biometrics → Remaining → Insights → Plan → Recent Meals).
- **Legible Weekly Meal Plan Fonts**: Increased readability of the weekly calendar on the Plans page by eliminating tiny, hardcoded inline font sizes on meal slots and dynamically applying crisp CSS-defined sizing (increasing slot labels to `10px` and slot meal titles to `13px` with `1.4` line-height).
- **Shared Logo Redesign (`LumaLogo.tsx`)**: Replaced the abstract glow mark with the new sun-over-hill logo artwork and propagated it across the login shell and app chrome through the shared logo component.
- **Global Logging Drawer (`LogSheet.tsx`)**: Created a premium sliding glassmorphic drawer for high-fidelity multi-modal ingestion.
  - **Voice Logging**: Integrated browser-native `MediaRecorder` connected to the `/log/meal/voice` endpoint using Whisper STT and LLM plate extraction.
  - **Barcode Scanner**: Added full QR/barcode camera lookup (`html5-qrcode`) and manual code lookup with Open Food Facts (OFF) query integration.
  - **Fuzzy Search**: Enabled pg_trgm search of foods from backend database.
  - **Plate Composition**: Interactive review, adjustments of portions/items, and nutrition overview before finalization.
- **Weekly Meal Planning API (`plan.py`)**: Implemented generation, active plan slots, slot swapping (AI-driven alternatives), shopping list checkboxes, and single-click "Log as Eaten" transitions.
- **Adherence Metrics Dashboard (`today.py`)**: Connected backend query structures to aggregate biometrics and daily macronutrient rollups against user profile goals (sat fat, soluble fiber, calories).
- **TypeScript Optimization**: Rectified all strict compiler checks, resolved unused imports, and enabled clean build generation for production.

### Changed
- **First-Run Operator Onboarding Restored**: Repositioned browser-based first-user account creation as the default workflow across setup docs, Make help text, and local setup output; `make seed` is now documented as an optional bootstrap/recovery utility rather than the primary path.
- **Auth Bootstrap Error Handling**: Auth endpoints now return explicit service-unavailable errors when the database is unavailable, uninitialized, or misconfigured, and the login screen surfaces those startup failures directly instead of showing a generic internal server error.
- **Frontend App Icons & Mock Bootstrap**: Fixed mock API initialization ordering, updated app icon/manifest references to existing public assets, and aligned planner copy from "Claude" to "Luma".
- **Brand Guide Compliance Sweep (Frontend UI)**: Removed emoji and symbol glyph usage from interactive surfaces, replaced meal-slot pictograms with Lucide icons, normalized icon stroke usage, applied safe-area handling to the mobile bottom nav, shifted non-logo/non-FAB sky+sun accents to sky-only gradients, converted route-level hardcoded UI colors to design tokens, and aligned Today insight cards with the specified left-accent border treatment.
- **Container-Free LLM SDK Integration**: Migrated the AI pipeline from the standalone LiteLLM proxy container to the in-process `litellm` Python SDK for direct connection to local/remote LLM nodes, reducing memory footprint and network routing complexity.
- **Dynamic Model Agnosticism**: Decoupled AI model configuration into environment variables (`FOOD_EXTRACTOR_MODEL`, `MEAL_PLANNER_MODEL`, `LOCAL_AI_API_BASE`), allowing the application to utilize any local or remote OpenAI-compatible completion node directly using `LOCAL_AI_API_BASE`.
- **Argon2 Constant-Time Verification**: Fixed the timing oracle mitigation by generating a syntactically correct, pre-compiled dummy hash to avoid verification decoding exceptions.
- **PWA Route Polish**: Wired the `AdherenceRing` visualization and meal planners to support live data fallbacks.
- **Login Visual Edge Smoothing**: Softened abrupt blur seams on the auth view by feathering atmospheric gradients and refining glass-card edge rendering.
- **Container Dev Hot Reload**: Added a profile-scoped `web-dev` service in Compose plus container-aware Vite proxy settings for live UI preview while backend services run in Docker.
- **Developer Command Shortcuts**: Added a root `Makefile` with quick start targets for `prod`, `dev`, `down`, `rebuild`, migrations, seeding, and logs.
- **Settings Sign-Out Action**: Added a frontend logout control in settings that calls `/auth/logout`, clears session state, and returns to login.
- **Today UI Mock Data Mode**: Added frontend mock utilities and sparse-data fallback for the Today view, plus `VITE_USE_MOCK_DATA=1` support for consistent UI tuning in dev.
- **App-Wide Frontend Mock Mode**: Added a centralized mock API layer for auth, today, trends, and plan endpoints so all major routes can be fully populated for design iteration without backend dependence.
- **Frontend Naming Cleanup**: Renamed the top-level `web/` folder to `frontend/` and aligned Compose/Makefile dev service naming to `frontend-dev` for clearer infrastructure mapping.
- **Per-User Measurement Units**: Added account-scoped measurement preference support with a new Settings switcher for `metric` or `imperial`, persisted via backend preferences API and mirrored in mock mode.

### Removed
- **LiteLLM Docker Container & Configs**: Deprecated and deleted the `litellm` container service from `compose.yml` and the local `./litellm/` directory with its configuration files, reducing the running container footprint.
- **Obsolete Parameters**: Removed `LITELLM_BASE_URL` and `LITELLM_MASTER_KEY` variables in favor of direct `LOCAL_AI_API_BASE` integration.

## [Phase 0] - Foundations

### Added
- **Docker Compose Orchestration**: Scaled the entire self-hosted stack containing PostgreSQL/TimescaleDB, Redis, LiteLLM, Whisper, Nginx, and FastAPI.
- **Alembic Database Schema**: Fully synchronized all 13 relational tables and 3 hypertables for high-density biometric logs, plan slots, and alerts.
- **Operator Authentication**: Custom Argon2id password-hashing with secure HttpOnly cookies, JWT management, and multi-user registration/creation prompts.
- **HMAC Signatures for Ingest**: Integrated secure SHA256 signatures for fast external data pipelines (HAE).
